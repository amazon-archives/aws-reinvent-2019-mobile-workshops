#Amplify Console build file
version: 0.1
frontend:
  phases:
    build:
      commands:
        - cd instructions
        - hugo --enableGitInfo
  artifacts:
    baseDirectory: instructions/public
    files:
      - '**/*'
  cache:
    paths: []

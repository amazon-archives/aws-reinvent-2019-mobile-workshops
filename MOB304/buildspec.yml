#Amplify Console build file
version: 0.1
frontend:
  phases:
    build:
      commands:
        - cd MOB304/instructions
        - hugo --enableGitInfo
  artifacts:
    baseDirectory: MOB304/instructions/public
    files:
      - '**/*'
  cache:
    paths: []

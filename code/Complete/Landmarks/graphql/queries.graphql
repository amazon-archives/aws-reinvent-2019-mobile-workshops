# this is an auto generated file. This will be overwritten
query GetLandmark($id: ID!) {
  getLandmark(id: $id) {
    id
    name
    category
    city
    state
    isFeatured
    isFavorite
    park
    coordinates {
      longitude
      latitude
    }
    imageName
  }
}
query ListLandmarks(
  $filter: ModelLandmarkFilterInput
  $limit: Int
  $nextToken: String
) {
  listLandmarks(filter: $filter, limit: $limit, nextToken: $nextToken) {
    items {
      id
      name
      category
      city
      state
      isFeatured
      isFavorite
      park
      coordinates {
        longitude
        latitude
      }
      imageName
    }
    nextToken
  }
}
